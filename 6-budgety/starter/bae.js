// function to update datalayer with a value
dataLayerPush: function(){
    var linkText;

    Id = data.allItems[type][data.allItems[type].length - 1].id + 1;

}
    // Return the new element
    return newItem;
    
}
//listener to call that function on click
document.querySelector(DOM.allListContainerClass).addEventListener('click', dataLayerPush);

function () {
    return document.getElementById('TextID');
  }